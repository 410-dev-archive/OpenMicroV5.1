/*

macro_exec.h

Team AVER [Advanced Vex EngineeRs]

2020.Sept.15

This header runs recorded macro.

Iteration Build: 1

*/

int macro_record_start() { // Start recording macro
	if (didMacroSaved) {
		// Overwriting macro.
		// User need to press designated key map to actually rewrite
	}else{
		// Start recording macro
	}
}
